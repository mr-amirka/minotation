// Сознательно НЕ импортируется из main.jsx — проверка, что onStart's
// blanket-скан src/ (не по графу зависимостей esbuild) всё равно находит
// эти токены.
export const detached = '<div class="w50% bgEEE"></div>';
