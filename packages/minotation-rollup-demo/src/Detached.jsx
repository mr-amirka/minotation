// Сознательно НЕ импортируется из main.jsx — проверка, что buildStart()'s
// blanket-скан src/ (не по графу импортов Rollup) всё равно находит эти токены.
export const detached = '<div class="w50% bgEEE"></div>';
