{
  "name": "minotation-runtime-vue",
  "version": "0.0.1",
  "description": "Vue adapter for minotation-runtime — lifecycle wiring for the framework-agnostic DOM runtime",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --config=./jest.config.js --no-coverage",
    "test:watch": "jest --watch --no-coverage --config=./jest.config.js",
    "test:coverage": "jest --coverage --config=./jest.config.js"
  },
  "peerDependencies": {
    "minotation": "0.0.1",
    "minotation-runtime": "0.0.1",
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.4.11",
    "typescript": "^5.0.0",
    "vue": "^3.5.39"
  }
}
